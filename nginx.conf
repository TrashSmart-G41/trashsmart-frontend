server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # This is the magic part for SPAs
    location / {
        # Try to serve the requested file directly, then a directory,
        # otherwise fall back to serving index.html.
        try_files $uri $uri/ /index.html;
    }
}
